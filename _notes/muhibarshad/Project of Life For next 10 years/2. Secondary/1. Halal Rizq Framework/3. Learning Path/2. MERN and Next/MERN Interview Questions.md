### **Promises** — _One-word: Asynchronous_

Handle async tasks with success/failure callbacks.

### **Event Loop** — _One-word: Scheduler_

Manages async operations by cycling through tasks.

### **Closure** — _One-word: Memory_

Function keeping access to outer variables.

### **Hoisting** — _One-word: Lifting_

JS moves declarations to the top before execution.

### **HOF (Higher-Order Function)** — _One-word: Wrapper_

Functions that take or return other functions.

### **Array Methods** — _One-word: Utilities_

Built-in functions to manipulate arrays (map, filter, etc.).

### **Spread Operator** — _One-word: Expand_

Expands arrays/objects into individual elements.

### **Rest Operator** — _One-word: Collect_

Groups remaining values into an array.

### **var** — _One-word: Function-scoped_

Old declaration with function scope and hoisting issues.

### **let** — _One-word: Block-scoped_

Preferred variable with block-level scope.

### **const** — _One-word: Immutable_

Block-scoped, cannot be reassigned.

### **Callback Function** — _One-word: Deferred_

Function passed to run later.

---

# 🔥 **REACT**

### **HOC (Higher Order Component)** — _One-word: Wrapper_

Function that adds behavior to components.

### **IIFE** — _One-word: Auto-execute_

Function executed immediately after creation.

### **Lifecycle Methods** — _One-word: Phases_

Component creation → update → destroy.

### **Class Components** — _One-word: Stateful_

React components using classes and lifecycle methods.

### **Functional Components** — _One-word: Hook-based_

React components using hooks.

### **useEffect** — _One-word: Side-effects_

Runs code after render (fetch, timers).

### **useLayoutEffect** — _One-word: Sync_

Runs before paint for DOM-dependent updates.

### **Hooks** — _One-word: Features_

Functions that add state/effects to components.

### **Props Drilling** — _One-word: Passing_

Passing data through multiple nested components.

### **Optimization** — _One-word: Efficiency_

Reduce unnecessary re-renders and computations.

### **Context API** — _One-word: Global_

Share data globally without prop drilling.

### **Redux Toolkit (RTK)** — _One-word: State_

Centralized predictable global state management.

### **RTK Query** — _One-word: Fetching_

Data fetching + caching for Redux Toolkit.

### **Thunk** — _One-word: Async_

Middleware to run async logic in Redux.

### **Event Bubbling** — _One-word: Upward_

Event moves from child → parent.

### **Event Delegation** — _One-word: Delegate_

One parent handles events of many children.

### **e.preventDefault()** — _One-word: Block_

Stops default browser behavior.

### **stopPropagation()** — _One-word: Stop_

Stops event from bubbling.

### **Next.js Cache** — _One-word: Store_

Stores data at build or runtime.

### **fetch (Next.js)** — _One-word: Request_

Server/client API calls with caching.

### **Server-side Rendering (SSR)** — _One-word: Pre-render_

HTML generated on server per request.

### **Hydration** — _One-word: Activate_

Attach React to static HTML from SSR.

### **Server-side Error** — _One-word: Backend_

Failure occurring before HTML reaches browser.

### **Client-side Error** — _One-word: Frontend_

Failure in browser runtime.

### **Local Storage** — _One-word: Persistent_

Long-term key/value storage (5MB).

### **Session Storage** — _One-word: Temporary_

Clears when tab closes.

### **Cookies** — _One-word: Auth_

Small browser data sent with every request.

### **React Window** — _One-word: Virtualize_

Render only visible list items for performance.

### **Debouncing** — _One-word: Delay_

Wait until user stops typing before running function.

### **React.memo** — _One-word: Cache_

Skip re-render if props unchanged.

---

# 🔥 **NODE / BACKEND**

### **MVC** — _One-word: Structure_

Model-View-Controller separation.

### **Controller** — _One-word: Logic_

Handles requests and responses.

### **Middleware** — _One-word: Filter_

Runs before controller (auth, logs).

### **Rate Limiting** — _One-word: Limit_

Restrict number of requests per client.

### **Throttling** — _One-word: Control_

Allow requests at a fixed interval.

### **Load Balancer** — _One-word: Distribute_

Split traffic across servers.

### **Nginx** — _One-word: Proxy_

Reverse proxy + load balancer.

### **Multer** — _One-word: Uploads_

Middleware for handling file uploads.

### **Monolithic** — _One-word: Single-unit_

Entire app in one codebase.

### **Microservices** — _One-word: Distributed_

App split into independent services.

### **Redis Optimization (Caching)** — _One-word: Speed_

Store frequent data for fast access.

### **API (REST)** — _One-word: Resources_

Standard JSON endpoints.

### **API (SOAP)** — _One-word: Protocol_

XML-based strict web service protocol.

### **ORM** — _One-word: Mapper_

Convert database tables → JS objects.

---

# 🔥 **DATABASES & SECURITY**

### **JWT Authentication** — _One-word: Token_

Sessionless secure user login tokens.

### **Authorization** — _One-word: Access_

Controls what a user can do.

### **Sockets.io** — _One-word: Real-time_

Bidirectional live communication.

### **Webhooks** — _One-word: Notify_

Server sends events to another server.

### **Queues (RabbitMQ)** — _One-word: Buffer_

Store tasks to process asynchronously.

### **Database** — _One-word: Storage_

Structured data system for applications.